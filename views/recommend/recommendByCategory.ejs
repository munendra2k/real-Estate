<!-- recRoomsByCategory -->
<% for( let i=recRoomsByCategory.length-1;i>=0;i-- ) { %>
<div class="card mb-3 gap-1 p-1" style="width: 22rem">
  <a href="/showmore/<%=recRoomsByCategory[i]._id%>">
    <img
      src="<%= recRoomsByCategory[i].image %>"
      class="card-img-top rounded-1"
      alt="<%= recRoomsByCategory[i].image %>"
      style="height: 15rem" />
  </a>
  <div class="card-body">
    <h5 class="card-title d-flex justify-content-between">
      <%=recRoomsByCategory[i].title%>
      <!-- room price price symbol(&#8377;)-->
      <span
        >&#8377;<strong
          ><%= recRoomsByCategory[i].price.toLocaleString("en-IN") %></strong
        ></span
      >
    </h5>
    <p class="card-text">
      <span>Category</span
      ><strong class="p-2"><%= recRoomsByCategory[i].category %></strong>
    </p>
    <p class="card-text">
      <span>Owner:</span
      ><strong class="p-2"><%=recRoomsByCategory[i].owner.name%></strong>
    </p>
    <br />
    <p class="col-5 text-truncate"><%=recRoomsByCategory[i].description%></p>
    <a href="/showmore/<%=recRoomsByCategory[i]._id%>">More ...</a><br />
    <p class="d-flex justify-content-between">
      <span
        ><%= recRoomsByCategory[i].city %>&sbquo;<%= recRoomsByCategory[i].state
        %>&sbquo;<%= recRoomsByCategory[i].zipCode %></span
      >
      <span><%= recRoomsByCategory[i].createdAt.toDateString().slice(4)%></span>
    </p>
  </div>
</div>
<% } %>
