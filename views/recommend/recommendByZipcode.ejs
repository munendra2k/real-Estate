<!-- recRoomsByZipcode -->
<% for( let i=recRoomsByZipcode.length-1;i>=0;i-- ) { %>
<div class="card mb-3 gap-1 p-1" style="width: 22rem">
  <a href="/showmore/<%=recRoomsByZipcode[i]._id%>">
    <img
      src="<%= recRoomsByZipcode[i].image %>"
      class="card-img-top rounded-1"
      alt="<%= recRoomsByZipcode[i].image %>"
      style="height: 15rem" />
  </a>
  <div class="card-body">
    <h5 class="card-title d-flex justify-content-between">
      <%=recRoomsByZipcode[i].title%>
      <!-- room price price symbol(&#8377;)-->
      <span
        >&#8377;<strong
          ><%= recRoomsByZipcode[i].price.toLocaleString("en-IN") %></strong
        ></span
      >
    </h5>
    <p class="card-text">
      <span>Category</span
      ><strong class="p-2"><%= recRoomsByZipcode[i].category %></strong>
    </p>
    <p class="card-text">
      <span>Owner:</span
      ><strong class="p-2"><%=recRoomsByZipcode[i].owner.name%></strong>
    </p>
    <br />
    <p class="col-5 text-truncate"><%=recRoomsByZipcode[i].description%></p>
    <a href="/showmore/<%=recRoomsByZipcode[i]._id%>">More ...</a><br />
    <p class="d-flex justify-content-between">
      <span
        ><%= recRoomsByZipcode[i].city %>&sbquo;<%= recRoomsByZipcode[i].state
        %><%=recRoomsByZipcode[i].zipCode %></span
      >
      <span><%= recRoomsByZipcode[i].createdAt.toDateString().slice(4)%></span>
    </p>
  </div>
</div>
<% } %>
